import java.util.Scanner;

public class printingShortestCommonSupersequence {
    public static void main(String[] swami){
        Scanner s = new Scanner(System.in);
        String s1=s.nextLine();
        String s2=s.nextLine();
        // we will find first the longest common subsequence between the 2 strings and then we will find the supersequence using the information in the dp array
        // complete this printing part later

    }
}
