import java.util.*;
public class minimumCostToCutTheStick {
    // in this problem , we cut the stick at the indexes given and then find the minimum cost to cut the stick , this minimum cost depends on the order in which the stick is being cut and if we kept the cuts in a proper order , the cost will be minimum
    // this is a problem on dp on partition
    public static void main(String[] swami){
        Scanner s=new Scanner(System.in);
        int n=s.nextInt();
        int[] a=new int[n];
        for(int i=0;i<n;i++){
            a[i]=s.nextInt();
        }

    }
}
